
//import { useParams } from "react-router-dom"
const ShowAddedBlog = ({ userArray }) => {

  //const userId = useParams().id
  /* userArray.find(u => u.id === userId) */
  //const user = []

  return (
    <div>
      <h2>added blogs</h2>
      {/* <ul>
        {
          user.blogs.map(b =>
            <li key={b.id}>{b.title}</li>
          )
        }
      </ul> */}
    </div>
  )
}

export default ShowAddedBlog